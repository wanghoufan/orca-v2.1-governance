# ORCA V2.0 重塑说明｜正式版 2026-09-09（相对 V1.10 的删减清单）

V1.10 封存不动。本文件只描述 2.0 正式版的取舍（历史文件）。

## 1. 模型一句话切（已落地本草稿）
- 新增根 `USER_MODEL_OVERRIDE.md`，你改表或窗口说一句即生效，下一派用新模型。
- builder 当前主力 `codex/gpt-5.6-terra / medium`，换谁你说了算，改表即生效。
- 仅保留：精确ID、池子隔离（TM只走GO）、花钱cap。30例harness、Promotion、sync、run-all应急时全免。

## 2. 迁移拷+一句话整理（已落地：迁移整理提示词.md）
- 砍掉 ORIGIN/APPLIED/指纹/manifest/STALE DENY。模板文件夹拷进老项目即 sidecar。
- 业务原地不动，能跑优先；整理只分类+输出《归位表》，搬了会broken就留原地+记引用路径。
- 仅保留 `GOVERNANCE_VERSION`（2.1 包内实物为 2.1），升级只提示不阻断。

## 3. 派工一句话开工 + 内联监督 + 单点对接（已落地：编排者提示词.md）
- 编排者（Task Manager）一句话接管：拆Task→派 Builder/产品/QA→收结果→推下一步。
- 监督者内联：只对编排者说话（打回/催工/判过没过），P0没完不准收工，每轮末三行心跳（目标/剩P0/下一步）。
- 对你永远单点：只有编排者喊你（阶段完+下阶段问一句行不行）；监督者平时静默，仅编排者失联时替喊一声。

## 4. 经验一句话（已落地：经验一句话.md）
- 经验记录者是编排者派的临时角色，收工时追加一句到 `经验一句话.md`，下次开工先读，不做 Learning Event/Promotion/升级。
