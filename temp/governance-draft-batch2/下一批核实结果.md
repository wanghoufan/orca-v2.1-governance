# ORCA 派工审计 · 下一批核实结果（2026-09-26）

> 承接《ORCA派工审计-核实结论与整改任务书》的 P0-1 / P1-5 / P1-2 三项。证据取自 1.Active 各项目账本与 docs。

## 一、P0-1 复原 028「ChangeB」打回链

**证据（028 账本 + git 0:1）**
- `ChangeB B-1+B-2实现`：builder，`rework=1`，备注「首派超时但落盘；返工补 Key 测试＋MATRIX-PLAN＋1 行 T166」。
- `ChangeB QA回归`：qa，`result=FAIL`，「沙箱 EPERM；虚报更新 QA 文档 1 次」。
- `ChangeB E2E补跑`：qa 本窗口（TM 接管），PASS。
- `ChangeB终检`：supervisor，`rework=2`，备注「**2 次打回**（第 2 次因子虚报）后 PASS 放行」，`escalated=NO`，note「未升级：打回原因已消除且终检 PASS」。
- git：`cce4306`（Change B 新 RC，reviewer+qa+supervisor PASS）为收口提交，打回过程不入 git。

**结论（不预设、不追个人）**
- **规则被触发与否无法从账本坐实**：supervisor 自记 2 次打回，但账本的 `rework` 字段**没标"谁打回、打回谁"**，"同一 Task"也未定义粒度（builder 交付？还是整个 ChangeB？）——因此不能判定"该升没升"。
- **真正暴露的是规则缺陷**，不是个人过失：①计数单元未定义；②打回来源不可机器计数；③"QA 挂不算"与"因 QA 问题被 supervisor 打回"边界不明；④存在"原因消除可免升"的**事实操作**，但规则里没有。

## 二、P1-5 逐项核验

| 项 | 结论 | 证据 |
|---|---|---|
| 022 TM 代执行 | 3 次事件（D8/D9/D10，非六条）。**D8** 有旁证：HANDOFF「D8 单行撤销确认，用户明确要求」；**D9**「用户拍板近似后」；**D10 无注明 → 待补** | 022 两账 + HANDOFF:11 |
| 028 PASS 疑点 | `RC清障三件` `result=PASS` 但 note「待评审初版；产品阻塞：局中无『真离开』入口」→ **角色交付 PASS、任务链未验收**，正确状态应为 `chain_status=OPEN` | 028 TASK:60 |
| 028 QA 虚报 | 现象已入账（「虚报更新 QA 文档 1 次」）；原始文档改动对照未获，**待核** | 028 DISPATCH:103 |
| TM 接管 29 条 | 绝大多数为 028 codebuddy 超时兜底 + 沙箱阻塞 QA 补跑 → **通道问题**，非越权；QM 边界已入草案 | 各项目账本 |

## 三、P1-2 codebuddy 超时策略

- 实践已验证有效：028「首派超时但落盘」→ 直接验（不重派）；027 曾「deepseek/glm 各超时 10 分钟零产出」→ 转 TM/本窗口。
- 已写入草案（override builder 行）：**超时先查工作区落盘——已落盘直接验，未落盘续派或转备通道**。

## 四、本批治理草案（待用户审阅）
1. **升级口径明确**（AGENTS 升级节）：定义计数单元＝同一 task id（不按角色拆）、只数 supervisor FAIL 且 DISPATCH 记账可数、QA 自身 FAIL 不计但致返工则计；**取消"原因消除可免升"默认例外**（需免升由用户在当次拍板）。
2. **TM 代做边界成文**（AGENTS 派工顺序）：真机QA直驱＝合规／通道兜底＝须记 `executed_by=task-manager`＋原因＋≥2 次上报／越权代做＝违规。
3. **codebuddy 超时策略**（override builder 行）。

## 五、需用户拍板
1. **升级免升**：同意"无默认免升、要免升须你当次拍板"吗？（草案按此写）
2. **028 既往**：既然账本无法坐实，是否**不追既往**、只从新口径起算？（建议：是）
3. 草案其余两处是否有异议。
