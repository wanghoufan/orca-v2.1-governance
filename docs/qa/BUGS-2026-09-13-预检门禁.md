# BUGS｜P1 预检门禁 QA 回归（2026-09-13）

| Bug ID | Priority | Stage P0 Blocking? | Repro | Status | Current Task | 备注（截图/日志一句） |
|---|---|---:|---|---|---|---|

## Fix Attempt Fingerprint

- Task ID: P1 预检门禁回归（真机QA会话能力预检判据＋Canary口径，只读）
- Root Cause Hypothesis: P1 修订改 `docs/roles/qa.md` 与 `docs/qa/BUGS.template.md`（预检枚举＋`ok=true` 判据＋像素差口径），须回归证明七态枚举、判据 token 与两包/ZIP 一致性未坏。
- Approach: 只读验证。`rg`/`grep`/`diff`/`python`（zipfile+md5）逐项取证，不改任何文件、不改业务。
- Files Changed: 仅新建本报告 `docs/qa/BUGS-2026-09-13-预检门禁.md`；其余全仓一字未动（本轮 qa.md/BUGS.template.md 改动系 TM 既有 P1 修订，非本 QA 所改）。
- Verification: 见下方“回归记录”五项，全部实测取证。
- Failure Reason: 无（P0=0/P1=0；四项 P2 均为口径观察项，不拦门）。
- Difference From Previous Attempt: 上轮回归域为 C1 账本/watchdog/路由（`BUGS-2026-09-12-Regression.md`）；本轮为 P1 预检门禁判据域。

> Attempt ID / Dispatch ID / Model-Backend 系字段 2.0 已废弃，不填（模型轨迹记账本）。

## 回归记录（P1 预检门禁，只读）

- 派工槽位：role=qa｜model=`deepseek-v4.1-flash`｜used主｜runtime=codebuddy，备用=无（MANUAL_ONLY）。本轮纯只读（`rg`/`grep`/`diff`/`python zipfile+hashlib`），未发起 codebuddy 活派工，故 `-y` N/A；无 GO 调用。
- 被检对象：`docs/roles/qa.md`、`docs/qa/BUGS.template.md`（母版＋两包）。

### ① 枚举 7 态齐全（PASS）

- `docs/roles/qa.md:5` 与 `docs/qa/BUGS.template.md:27` 两处均含完整 7 态枚举，逐字一致：`PASS / BLOCKED_TOOL_NOT_INJECTED / BLOCKED_ORCA_APPROVAL / BLOCKED_RUNTIME / BLOCKED_OS_PERMISSION / FAIL_UNVERIFIED_ACTION / NOT_VERIFIED`（各 1 次完整串命中）。
- 两处均带“禁 `FAIL_MODEL_ACTION`”在位；`FAIL_MODEL_ACTION` 在母版主动域零作为合法枚举出现（仅出现在“禁”句中）。

### ② ok=true 判据两处（PASS）

- `docs/roles/qa.md:5` 与 `docs/qa/BUGS.template.md:9` 两处均落判据：“`ok=true/exit 0/工具调用成功`但无状态或像素变化记 `FAIL_UNVERIFIED_ACTION`”。
- 两处语义一致，无第三处歧义写法。

### ③ 判据 token 四处一致（PASS）

- `FAIL_UNVERIFIED_ACTION` 在母版主动域共 4 处，语义一致：`docs/roles/qa.md:5`（预检总结论）、`docs/qa/BUGS.template.md:9`（预检节判据）、`docs/qa/BUGS.template.md:25`（滚动像素差为零专项）、`docs/qa/BUGS.template.md:27`（预检最终结论枚举）。
- 4 处均为“无状态/像素变化即记”同一口径，无冲突。

### ④ 母版 vs 包 ZERO（PASS）

- `diff docs/roles/qa.md 新项目模板包/docs/roles/qa.md` → EXIT:0；`diff docs/roles/qa.md 老项目迁移模板包/docs/roles/qa.md` → EXIT:0。
- `diff docs/qa/BUGS.template.md 新项目模板包/docs/qa/BUGS.template.md` → EXIT:0；对老包同 → EXIT:0。
- 结论：预检门禁两文件在母版↔新包↔老包间**字节零差**（非预期差 ZERO）；母版对两包其余差异仅 AGENTS/README/Contract/提示词扁平包裸名等既定布局差，与本域无关。

### ⑤ ZIP vs 磁盘 ZERO（PASS）

- 新包 ZIP：`docs/roles/qa.md` ZIP==disk True（md5 `7f47cc98`，1235B）；`docs/qa/BUGS.template.md` ZIP==disk True（md5 `96054679`，2267B）。
- 老包 ZIP：两文件同上，md5/字节与磁盘一致（ZIP==disk True）。
- 结论：两分发 ZIP 内预检门禁文件与磁盘**零差**，非旧缓存（ZIP 时间戳 09-13 09:39）。

### 结论

- 结果：**PASS**；P0=0，P1=0。
- P2×4（均不拦门，口径观察项，放行不改）：
  - P2-1 HANDOFF 待补 §13（本轮 P1 修订落档节，现行 HANDOFF 末节为 §12，§13 未落，由 TM 随后补齐）。
  - P2-2 `PASS/全PASS` 措辞双轨放行（预检“全PASS才进正式QA”与 Canary“七项全过才写已启用”两种措辞并存，分属不同语境，判为合理放行，不统一）。
  - P2-3 `NOT_VERIFIED` vs `PENDING` 用途不同（预检“能力缺席”用 `NOT_VERIFIED`，Canary“七项未测”用 `PENDING`，二者语境不同，判为并存不改）。
  - P2-4 “判断UI状态”隐含落点（Canary 七项之一在预检节以“无副作用UI Canary”隐含承载，未单列，属口径注，不拦门）。

### 声明

- `USER_MODEL_OVERRIDE.md` 未动（`git status` 无该文件，`git diff` 空；本轮修订不涉模型表，10 行主备与执行通道列保持原状）。
- 本轮只读验证，未改业务代码：除新建本报告外全仓零改动（不 commit）。
