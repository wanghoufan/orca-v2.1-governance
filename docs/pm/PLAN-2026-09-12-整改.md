# PLAN｜ORCA V2.1 模型与双阶段治理整改拆活（2026-09-12）

> 方案原文（只读不改）：`/Users/zzymima0000/Downloads/大模型 HANDOFF/2026-09-12 丨 macOS 丨 ChatGPT 丨 ORCA V2.1模型与双阶段治理-整改方案 丨 V1.0.md`
> 版本标记：V2.2（2026-09-12用户定，Amendment转正）。

## Product Goal

按整改方案 §11 把整改拆成可派 Task，分三批执行：BatchA 模型整改独立验收 → BatchB 双阶段治理独立验收 → BatchC 回归＋同步＋ZIP。约束：BatchA Gate 未过不得进 B；不新增第 11 角色；Bridge 合同历史取证不动；watchdog 逻辑不动；版本号先不动。

## Current Stage

- Stage ID: 整改拆活（planner 输出，待 Task Manager 拍板派工）
- Goal: 输出可直接派工的 Task 清单，每 Task 有输入文件、验收 Gate、落盘位。

## Stage P0（P0=本 Stage 非做不可、缺它不算完的事项，由 planner 初定、Task Manager 拍板）

- [ ] P0-1：BatchA Task 覆盖方案 §3＋§4（模型表＋角色卡模型行＋AGENTS 模型句＋双账本示例行＋Model Gate）。
- [ ] P0-2：BatchB Task 覆盖方案 §5＋§6＋§7（AGENTS 两阶段＋三口令＋7 张卡增量＋双新模板＋HANDOFF 六字段＋README＋协议顶注＋治理测试）。
- [ ] P0-3：BatchC Task 覆盖方案 §8＋§9（回归＋两包同步＋ZIP 重建校验）。
- [ ] P0-4：每 Task 写清输入文件、验收 Gate、落盘位；Batch 间门禁写死（A→B 门禁）。
- [ ] P0-5：§13 禁止事项逐条落到 Task 约束里（无第 11 角色、无额度服务、无 Spark Gate、TM/supervisor 不合并、Bridge/watchdog/协议正文不动、版本号不动）。

## In Scope

- 方案 §3（模型表 10 行＋GO MANUAL_ONLY＋V4.1 统一＋Bridge standby）、§4（Model Gate）、§5–§7（双阶段＋测试）、§8（回归）、§9（同步＋ZIP）。
- 母版包根及 docs/ 主动文件；两包同步与 ZIP 重建只在 BatchC 做（BatchA/B 不碰两包，避免漂移排错混杂）。

## Out of Scope

- Bridge 合同（`V2.1_BRIDGE_INTEGRATION_CONTRACT.md`）历史取证任何改动（只读核对）。
- `scripts/orchestration/coordinator-watchdog-standalone.sh` 逻辑改动（只跑 `bash -n` 回归）。
- 持续推进协议 1292 行正文重写（只加顶注一行）。
- GPT/GO 额度自动读取、配额状态机、新 Registry、额度服务（方案 §2.2 明确不做）。
- GOVERNANCE_VERSION 升 2.2（留用户拍板；本轮只在变更说明记 Two-Phase Amendment）。
- 历史审查报告 / 历史 BUG / 历史 HANDOFF 改动；V1.10/V2.0 封存；secrets；push（红线）。

## Task Breakdown

| Task ID | Priority | Role | Status | Notes |
|---|---|---|---|---|
| A1 模型表重写 | P0 | builder | 已完成PASS | USER_MODEL_OVERRIDE；BatchA入口 |
| A2 角色卡模型行批量改 | P0 | builder | 已完成PASS | 9 卡仅模型定位行，职责留 BatchB |
| A3 AGENTS 模型句最小改 | P0 | builder | 已完成PASS | 只动模型冲突句，两阶段留 BatchB |
| A4 双账本示例行＋Model Gate 验证 | P0 | qa | 已完成PASS | 示例行更新＋§4 全部检查；BatchA 门禁 |
| B1 AGENTS 两阶段治理 | P0 | builder | 已完成PASS | 门禁：A4 PASS 后方可派 |
| B2 编排者提示词三口令 | P0 | builder | 已完成PASS | 三口令＋PLAN/DEVELOP 双流程 |
| B3 TM＋supervisor 卡增量 | P0 | builder | 已完成PASS | Phase owner / Phase Integrity |
| B4 planner＋product-reviewer 卡 | P0 | builder | 已完成PASS | 产品 Planner / Research Reviewer（ID 不变） |
| B5 builder＋reviewer＋qa 卡 | P0 | builder | 已完成PASS | Phase2 only / 独立 Session / Canary 口径 |
| B6 模板与 HANDOFF＋README＋协议顶注 | P0 | builder | 已完成PASS | 双新模板＋六字段＋三口令＋顶注一行 |
| B7 双阶段治理测试 | P0 | qa | 已完成条件PASS | 方案 §7 全项；输出 docs/qa/ |
| C1 回归测试 | P0 | qa | 待派 | 方案 §8；watchdog 只回归不改 |
| C2 两包同步＋ZIP＋版本标记 | P0 | builder | 待派 | diff 零容忍＋ZIP 校验＋HANDOFF 记一行 |

### A1｜模型表重写（builder）

- 目标：按方案 §3.1 重写主动表 10 行：TM=FREE（本窗口）／supervisor=V4.1+codebuddy／builder=V4.1+codebuddy／planner=Sol／reviewer=V4.1+codebuddy／qa=V4.1+codebuddy／product-reviewer=FREE（本窗口）／recorder=V4.1+codebuddy／neat=V4.1+codebuddy／senior=Sol；§3.2 加 `OPENCODE_GO = MANUAL_ONLY` 并删除一切 FREE→GO 自动 fallback；§3.3 V4.1 统一（CodeBuddy 主动路由只许 `deepseek-v4.1-flash`，`deepseek-flash` 只许出现在 Bridge 历史/standby 语境）；effort=high 写口头不进模型列。
- 输入文件：`USER_MODEL_OVERRIDE.md`（全文重写基线）；`V2.1_BRIDGE_INTEGRATION_CONTRACT.md`（只读核对三元组，不改）；方案 §3.1–§3.3。
- 落盘位：`USER_MODEL_OVERRIDE.md`（母版；两包同步留 C2）。
- 验收 Gate（静态，qa 在 A4 复验，builder 自检先过）：
  - `rg -n "opencode-go/" USER_MODEL_OVERRIDE.md` 主动表零自动 GO（仅 MANUAL_ONLY/历史说明文字可含字样）。
  - `rg -n "builder.*muse-spark.*free" USER_MODEL_OVERRIDE.md` 无匹配。
  - `rg -n "deepseek-v4|deepseek-flash" USER_MODEL_OVERRIDE.md` 主动路由仅 `deepseek-v4.1-flash`。
  - reviewer 非 Terra、qa 非 MiMo、product-reviewer 非 Luna、senior 主用 Sol。
- 约束：精确 ID 禁别名；不编 rank；Bridge 合同一字不动；GO 不得进备用列。

### A2｜角色卡模型行批量改（builder，一 Task 批量 9 卡）

- 目标：仅改模型定位行，不动职责定义（职责留 BatchB）：supervisor→V4.1+codebuddy；builder→V4.1+codebuddy（保留 -y/正文判据引用，不改逻辑）；code-reviewer→V4.1+codebuddy；qa→V4.1+codebuddy；experience-recorder→V4.1+codebuddy；neat-freak→V4.1+codebuddy；senior-expert→Sol（只接升级）；planner→Sol（加一句 Phase1 定位预告，详细规则留 B4）；product-reviewer→FREE（加一句“职责改 Research Reviewer 留 BatchB”，详细留 B4）。task-manager 卡不动（已是“读表”句式）。
- 输入文件：`docs/roles/supervisor.md`、`docs/roles/builder.md`、`docs/roles/code-reviewer.md`、`docs/roles/qa.md`、`docs/roles/experience-recorder.md`、`docs/roles/neat-freak.md`、`docs/roles/senior-expert.md`、`docs/roles/planner.md`、`docs/roles/product-reviewer.md`；`USER_MODEL_OVERRIDE.md`（A1 新版为准）。
- 落盘位：上述 9 张角色卡原位（母版；两包同步留 C2）。
- 验收 Gate：每卡模型行与 A1 新表一致；`rg -n "opencode-go/|gpt-5.6-terra|gpt-5.6-luna|mimo-v2.5|muse-spark-1.3-contributor-free" docs/roles/` 仅允许 planner/senior=Sol、product-reviewer=FREE，其余 V4.1；supervisor 账本校验块、builder 通道无关段原文不动（A4 回归抽查）。
- 约束：不新增角色；不改 watchdog；Phase 规则一句不加（留 BatchB，避免 A/B 混杂）。

### A3｜AGENTS 模型句最小改（builder）

- 目标：只解决与新模型表直接冲突的句子：模型节指向新表＋GO MANUAL_ONLY（自动切备仅 FREE→B 同通道语义内按表切，禁切 GO）；Bridge 降 standby 一句（历史证据不动）；其余派工顺序/升级/账本/缓存/红线一字不动；两阶段治理一句不加（留 B1）。
- 输入文件：`AGENTS.md`；`USER_MODEL_OVERRIDE.md`（A1 新版）。
- 落盘位：`AGENTS.md`（母版；两包同步留 C2）。
- 验收 Gate：`rg -n "deepseek-v4|deepseek-flash|opencode-go/" AGENTS.md` 仅 MANUAL_ONLY/standby 语境；升级节“2 次打回/P0-hard 升 senior”原文保留；红线（换模型用户定、不碰 secrets、不改封存）不动。
- 约束：最小 diff；版本号不动。

### A4｜双账本示例行＋Model Gate 验证（qa，前置：A1–A3 完成）

- 目标：①更新双账本 `_example` 行模型 ID 以匹配新表（schema 枚举不动）；②跑通方案 §4 全部 Model Gate 并出报告；③任一 Gate FAIL 即 BatchA 不通过，不得进 BatchB。
- 输入文件：`docs/model/TASK-MODEL-LOG.jsonl`、`docs/model/DISPATCH-LOG.jsonl`（各仅 1 行 `_example`）；A1–A3 产出；方案 §4.1–§4.4。
- 落盘位：`docs/model/TASK-MODEL-LOG.jsonl`（示例行模型换新表对应 ID，如 builder→`deepseek-v4.1-flash`，其余类推；schema 键不动）、`docs/model/DISPATCH-LOG.jsonl`（示例行 `model/runtime` 对齐新表）；测试报告 `docs/qa/BUGS-2026-09-12-ModelGate.md`（照 BUGS.template.md，含 §4.4 十五项 checklist PASS/FAIL＋smoke 记录＋GO negative 记录＋未验证项）。
- 验收 Gate（Model Gate＝BatchA 门禁）：
  - 静态 §4.1 四项全 PASS（含 A1 的 4 条 rg）。
  - Smoke：FREE 最小只读（TM/product-reviewer 槽位）PASS；CodeBuddy `codebuddy --model deepseek-v4.1-flash --effort high --help` rc=0＋最小单发正文 PASS；supervisor/builder/reviewer/qa 各至少一次只读或 dry-run（-y 显式标注）；Sol 只做路由静态确认（有近期 PASS 证据不重复烧额度，禁 Benchmark）。
  - GO Freeze negative：模拟 FREE 不可用→TM 切表内 V4.1 备或停派找人，不得自动进 GO。
  - 账本：双 JSONL supervisor 校验块好例 exit 0、坏例 exit 1（沿用卡内命令）。
- 约束：测试烧 Sol 额度禁超最小连接；发现模型挂 vs 任务挂要在报告写清（result 判定口径见 AGENTS 账本节）。

### B1｜AGENTS 两阶段治理（builder，前置：A4 PASS）

- 目标：按方案 §5–§6.1 改 AGENTS：顶部加 `PHASE_1_PLAN / WAITING_HUMAN_APPROVAL / PHASE_2_DEVELOP`；单线派工改为 Phase-aware（Phase1 禁 Builder/CodeReviewer/QA/业务改动/Release；Human Gate 不可自动跨越；Phase2 禁随意改 Plan）；product-reviewer 显示名改 Research Reviewer（ID 不变）；TM/supervisor 独立重申；Change Request A/B/C；保留 2 次打回/P0-hard 升 Sol（Sol=senior-expert）。
- 输入文件：`AGENTS.md`（A3 版）；方案 §5.1–§5.2、§5.6–§5.7、§5.10、§6.1。
- 落盘位：`AGENTS.md`（母版；两包同步留 C2）。
- 验收 Gate：B7 中 Human Gate negative（未说“第二阶段，开发”派 Builder 被拒）PASS；`rg -n "PROJECT_PHASE|WAITING_HUMAN_APPROVAL|CHANGE_REQUEST|Research Reviewer" AGENTS.md` 全命中；9+1 角色数不变（`rg -c` 无新增角色文件）。
- 约束：9+1 不新增；TM/supervisor 不合并；无 Spark Gate；无额度状态机字段。

### B2｜编排者提示词三口令（builder，前置：A4 PASS，可与 B1 并行）

- 目标：按方案 §5.1、§6.2 重写 `docs/prompts/编排者提示词.md` 相关段：新增三口令 `第一阶段，计划／第二阶段，开发／变更请求：……`；PLAN 链（Planner→Research Reviewer→Planner→…→Readiness Gate→Human Gate）与 DEVELOP 链（Builder→Reviewer→QA→Supervisor→TM）；90 分达标自动停磨；用户不搬运反馈（TM 自动回传）；未经批准禁开发；Change C 触发 Controlled Reopen。
- 输入文件：`docs/prompts/编排者提示词.md`；方案 §5.1–§5.6、§5.10、§6.2。
- 落盘位：`docs/prompts/编排者提示词.md`（母版；两包同步留 C2）。
- 验收 Gate：三口令字面可grep；B7 的 Phase1 正向/Phase2 正向/Change A/B/C 用例按新提示词可执行；旧单线顺序句已改为 Phase-aware（无残留“planner→builder→…→product”默认链）。
- 约束：只改编排者提示词；协议正文、watchdog 不动。

### B3｜TM＋supervisor 卡增量（builder）

- 目标：TM 卡加 Phase owner：PLAN/DEVELOP 两套派工、Human Gate、Change A/B/C 分类、DEV_BASELINE 锁定、Readiness≥90 停循环、Planner/Reviewer 自动回传；supervisor 卡加 Phase Integrity 五查（PLAN 禁 Builder/QA 派工、WAITING 禁自动开发、DEVELOP 必有 DEV_BASELINE、C 类禁绕 Reopen、TM 停摆沿用 watchdog/恢复职责），账本校验块不动。
- 输入文件：`docs/roles/task-manager.md`、`docs/roles/supervisor.md`；方案 §6.3–§6.4。
- 落盘位：上述 2 卡原位（母版；两包同步留 C2）。
- 验收 Gate：B7 中 Readiness<90 继续 PLAN、≥90 进 WAITING、TM 停循环找 Human 三项 PASS；supervisor 双账本校验块原文 `bash` 好例 exit 0 / 坏例 exit 1 回归 PASS。
- 约束：supervisor 不改成 Planner/Reviewer；TM 不直写业务代码（原“不做”保留）。

### B4｜planner＋product-reviewer 卡（builder）

- 目标：planner 从“拆活”升级为产品 Planner（Phase1 主职责：目标/用户/功能/User Flow/技术初稿/风险/DoD/假设＋按 Review 多轮修订；Phase2 默认停用，仅 Controlled Reopen/用户明确重规划进入）；product-reviewer 标题改为 `# product-reviewer（Research Reviewer / 研究审查者）`（内部 ID 不变），职责按方案 §5.4（Researcher/Reviewer/Fact Checker/Devil's Advocate/Challenger＋外部验证优先＋四项强制输出），Phase2 默认不派。
- 输入文件：`docs/roles/planner.md`、`docs/roles/product-reviewer.md`；方案 §1.3–§1.4、§5.3–§5.5、§6.5–§6.6。
- 落盘位：上述 2 卡原位（母版；两包同步留 C2）。
- 验收 Gate：B7 Phase1 正向（Sol Planner 出 PRODUCT PLAN→TM 派 Reviewer→反馈自动回 Planner→用户不搬运）PASS；`rg -n "product-reviewer" docs/ AGENTS.md` 内部 ID 无改名残留断裂；无第 11 角色文件新增。
- 约束：不新增角色文件；Readiness 评分定义以 PRODUCT_PLAN 模板为准（卡内不另写一套字段，防 09-09 教训重演）。

### B5｜builder＋reviewer＋qa 卡（builder）

- 目标：builder 加仅 Phase2／读 DEV_BASELINE／禁扩范围／疑似 C 立返 TM／V4.1／两轮失败升级；reviewer 加 Phase2 only／V4.1 独立 Session（输入=Requirement+DEV_BASELINE+DoD+Diff+测试结果＋必要上下文；找错/回归/越界，不维护原方案；禁 Spark Gate）；qa 加 Phase2 only／V4.1／unit-build-lint-API-logs-regression-DoD＋真机 Canary 七项＋未过标 PENDING/NOT VERIFIED（禁编造支持）。
- 输入文件：`docs/roles/builder.md`、`docs/roles/code-reviewer.md`、`docs/roles/qa.md`；方案 §5.7–§5.9、§6.7–§6.9。
- 落盘位：上述 3 卡原位（母版；两包同步留 C2）。
- 验收 Gate：B7 独立 Reviewer 测试（Session 隔离＋PASS/FAIL＋FAIL 回链）PASS；两轮升级（V4.1 FAIL→整改→仍 FAIL→Sol senior）PASS（mock/dry-run 可，不烧 Sol）；qa Canary 未过不得写支持。
- 约束：builder 禁自扩范围；reviewer 禁直接改代码（原“不做”保留）；qa 禁顺手改代码。

### B6｜模板与 HANDOFF＋README＋协议顶注（builder）

- 目标：①新建 `docs/pm/PRODUCT_PLAN.template.md`（Phase1 专用，含方案 §6.10 全部字段：Plan Version/PROJECT_PHASE/Product Goal/Target Users/Problem/Core Value/User Flow/Functional Scope/Out of Scope/Technical Approach/Data-API/Key Assumptions/Competitor-Research/Risks/DoD/P0-P2/Human Decisions/Readiness Score/Review Round/PLAN_GATE）；②新建 `docs/review/RESEARCH_REVIEW.template.md`（§6.11 全部字段）；③`docs/pm/PLAN.template.md` 追加 `DEV_BASELINE:/CHANGE_REQUEST:`（其余不动，作 Phase2 Stage/Task Plan）；④`docs/handoff/HANDOFF.template.md` 加六字段＋枚举（PROJECT_PHASE: PLAN/WAITING_HUMAN_APPROVAL/DEVELOP；PLAN_GATE: IN_PROGRESS/READY_FOR_HUMAN_REVIEW/APPROVED；CHANGE_REQUEST: NONE/A/B/C；另 PLAN_VERSION/PLAN_READINESS_SCORE/DEV_BASELINE）；⑤`README.md` 加三口令＋模型口径（FREE=TM/Research、V4.1=开发主力、Sol=Plan+升级、GO manual-only）；⑥协议顶注：在 `docs/prompts/2026-09-02 丨 Orca 通用编排者持续推进协议 丨 V1.1.md` 顶部收编说明追加一句“两阶段治理、Human Gate、Change Request 与固定 9+1 定义优先于正文通用动态流程描述”（正文 1292 行不动）。
- 输入文件：`docs/pm/PLAN.template.md`、`docs/review/PRODUCT_BACKLOG.template.md`（兼容保留参照）、`docs/handoff/HANDOFF.template.md`、`README.md`、协议文件顶部收编说明；方案 §6.10–§6.14。
- 落盘位：`docs/pm/PRODUCT_PLAN.template.md`（新）、`docs/review/RESEARCH_REVIEW.template.md`（新）、`docs/pm/PLAN.template.md`、`docs/handoff/HANDOFF.template.md`、`README.md`、协议文件顶注（母版；两包同步留 C2）。
- 验收 Gate：双新模板字段齐全（逐项 grep）；HANDOFF 六字段＋枚举齐全；README 三口令字面可grep；`git diff --stat` 协议文件仅顶注行变更（正文零改）。
- 约束：PRODUCT_BACKLOG 保留兼容不删；不建额度字段；模板与卡不各写一套字段（Readiness 定义以 PRODUCT_PLAN 为准）。

### B7｜双阶段治理测试（qa，前置：B1–B6 完成）

- 目标：跑通方案 §7.1–§7.12（Phase1 正向／Readiness84+2×P1 留 PLAN／Readiness92 进 WAITING＋停循环＋找 Human＋禁 Builder／Human Gate negative 拒派 Builder＝P0／Phase2 正向＋DEV_BASELINE＋V4.1 主链／独立 Reviewer／两轮升级 Sol（mock 可）／Change A 留 DEVELOP 不召 Planner／Change B 更局部 DoD 不召 Sol／Change C 进 PLAN_REOPEN_REQUIRED＋局部暂停＋Sol+Reviewer+Human+新版本+新基线回 DEVELOP／supervisor 防停摆＋watchdog 唤醒不代做（有近期 PASS 证据可回归）／真机 Canary 七项（V4.1 无桌面能力则标 PENDING，不编造））。
- 输入文件：B1–B6 全部产出；方案 §7。
- 落盘位：`docs/qa/BUGS-2026-09-12-PhaseGate.md`（照 BUGS.template.md，逐项 PASS/FAIL＋复现＋未验证项）。
- 验收 Gate：7.4 Human Gate negative 必须 PASS（P0）；7.3/7.5/7.10（C 受控重开＋Human Approval＋新基线）必须 PASS；其余 FAIL 须注明任务挂还是模型挂并打回对应 Task（不断链累计 supervisor 打回数）。
- 约束：Sol 真烧额度仅最小连接（§4.2/§7.7 有近期证据免重测）；禁 Benchmark；禁为测试改 watchdog。

### C1｜回归测试（qa，前置：B7 PASS）

- 目标：方案 §8 四项：JSONL 双 validator 好例 exit 0／坏例 exit 1；watchdog `bash -n`（已部署才加 launchctl 三件套，只读唤醒验证，不改逻辑）；精确路由三元组（V4.1→codebuddy、Sol→codex、FREE→本窗口，无新别名）；全仓 `rg -n "opencode-go/" .` 无主动自动 fallback（仅历史文件/MANUAL_ONLY 允许）。
- 输入文件：`docs/model/*.jsonl`、supervisor 卡校验块、`scripts/orchestration/coordinator-watchdog-standalone.sh`（只读）、全仓模型引用；方案 §8。
- 落盘位：`docs/qa/BUGS-2026-09-12-Regression.md`（照 BUGS.template.md）。
- 验收 Gate：四项全 PASS；任一 FAIL 打回对应 Batch Task 修，不进 C2。
- 约束：watchdog 逻辑零改；Bridge 合同零改（rg 命中仅历史/standby 语境）。

### C2｜两包同步＋ZIP＋版本标记（builder，前置：C1 PASS）

- 目标：母版主动文件同步 `新项目模板包/`、`老项目迁移模板包/`（老包仅保留迁移专属提示词/README 差异，其余治理核心不漂移）；重建 `新项目模板包.zip`、`老项目迁移模板包.zip` 并校验含新文件非旧缓存；`diff` 非预期差零容忍；在 `docs/handoff/HANDOFF.md` 记本轮整改一行；变更说明记“V2.1 Two-Phase Amendment”（GOVERNANCE_VERSION 保持 2.1，2.2 留用户拍）。
- 输入文件：母版全部改动文件；两包现行文件；方案 §9–§10。
- 落盘位：两包对应文件＋两 ZIP＋`docs/handoff/HANDOFF.md` 记一行（commit 需编排者明确指令，默认不 commit、不 push）。
- 验收 Gate：`diff -r` 母版↔两包主动文件仅允许迁移专属差异；unzip -l 两 ZIP 含 PRODUCT_PLAN/RESEARCH_REVIEW 新模板＋新 HANDOFF.template；HANDOFF 记一行含 Amendment 标记；GOVERNANCE_VERSION 仍为 2.1。
- 约束：常驻同步口径（HANDOFF 记一行）；两包 ignored 不入库状态不动；版本号升级留用户决策。

## Risks

- Sol 烧额度：A4/B7 设最小连接＋历史证据免测，禁 Benchmark。
- A/B 混改排错难：门禁 A4 PASS 才进 B 写死；B1–B6 并行但 B7 必须等 B1–B6 齐。
- Bridge 历史证据被误改：A1/C1 双 Gate（rg 语境检查＋零改约束）。
- 模板与卡字段两套：Readiness 定义以 PRODUCT_PLAN 为准，卡内引用不重写（B4/B6 约束）。
- 两包漂移：同步只在 C2 一次性做，diff 零容忍。

## Human Decisions Needed

- 【请你决策】BatchA Model Gate 由谁判 PASS（TM 拍板还是你亲自看 A4 报告）？【说明】方案 §2 要求 A 过才进 B，建议你看一眼 A4 报告再放 B。
- 【请你决策】版本号是否升 2.2，还是保持 2.1 Two-Phase Amendment？【说明】方案 §10 推荐升 2.2，但本次约束先不动，需你一句话才升（C2 默认不动）。
- 【请你决策】Sol 无近期 PASS 证据时是否允许一次最小连接测试额度？【说明】A4/B7 默认为静态确认，需你批准才做最小单发。
